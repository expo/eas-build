build:
  name: Inputs
  steps:
    - run:
        name: Say HI
        inputs:
          name: Dominik Sokal
          country: Poland
          boolean_value: true
        command: echo "Hi, ${ inputs.name }, ${ inputs.boolean_value }!"
