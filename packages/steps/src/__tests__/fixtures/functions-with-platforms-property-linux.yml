functions:
  say_hi_linux:
    name: Hi!
    inputs:
      - name
    command: echo "Hi, ${ inputs.name }!"
    platforms: [linux]

  say_bye_linux:
    name: Bye!
    inputs:
      - name
    command: echo "Bye, ${ inputs.name }!"
    platforms: [linux]
